# CHANGELOG — Ops Project Suite

One line per suite-version bump, newest first. The current version lives in `ABOUTME.md`.
Format: `<version> | <date> | <what changed>`

---

0.7-kit | 2026-09-23 | ported master 0.7 — four frictions learned from the first two real closeouts, plus one workspace ruling: (a) a `Stakes: high` domain playbook may exceed the ~6KB ceiling when the only cut left is a judgment rule, size and reason recorded in a dated note at the top of the playbook (its boot list); (b) closeout's closed `prompt.md` keeps the template's four section headings and the `_Last updated:_` line, because the template (`templates/prompt-template.md`) fixes exactly those four sections and a closed summary in the same shape stays readable and comparable; (c) the high-stakes verification audit runs from wherever QA claims are stated (`changelog.md`, `todo.md`, the hub) when there's no `verification-log.md`, never skipped; (d) the stray-file sweep leaves governance and decision registers where they were created — the verification log at the project root, the data-room register under `working-docs/data-room/`, a proposals register wherever the playbook put it; (e) the workspace ruling: bulk inbound third-party document sets never enter the project folder — generalised from the corporate-cloud-store wording to a folder OUTSIDE the project (e.g. iCloud Drive), named at kickoff, with `working-docs/data-room/REGISTER.md` and `working-docs/data-room/REGISTER.sha256` (created at receipt, a held copy checked against it rather than "proved" intact)
0.6-kit | 2026-09-16 | forked for the starter kit: the shared knowledge store is replaced by a user-chosen projects folder ({lessons,playbooks,PROJECTS.md}); adversarial checks wired to the kit's reviewer and reviewer-gpt roles; identity generalised; round-1 review fold (privacy of the PROJECTS.md row, single evidence path, LibreOffice made conditional, financial rules reworded as per-exercise decisions, AGENTS.md rename, size ceilings)
0.5 | 2026-07-08 | baseline the kit fork was taken from

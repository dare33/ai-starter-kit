# PROMPT — <Project Name>

> **The single source of status and next actions.** If any other file disagrees with this
> one, this one wins — fix the other file.
> **Update atomically: rewrite this whole file every time — never patch one section.**
> Section-patching is how a file contradicts itself. Exactly these four sections, no
> banners or "resume here" blocks; ≤5 "Recently done" bullets (older → `changelog.md`);
> hard cap ~1.5KB — if it doesn't fit, it isn't status.

_Last updated: <date> (<by whom / what confirmed>)._

## Where we are

<One short paragraph. The complete current truth — counts, what's confirmed, what's not.
This paragraph is the only place status may be written. During a bulk run, write one
line — "bulk run in flight since <when>; authoritative progress: `verification-log.md`
tail" — and let that ledger carry per-unit progress (one fact, one home).>

## Next actions (in order)

1. <action — concrete enough to start cold>
2. <action>

## Open decisions for the owner

- <decision needed, with the options if known>

## Recently done

- <date> — <one line>
